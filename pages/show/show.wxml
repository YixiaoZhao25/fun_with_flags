<view class="header">
  <view class="header-name">Fun with Flags</view> 
</view>

<view class="card">
    <view class="card-title"> This is the flag from the country of</view>
      <view class="country-name">{{countries.name}}</view>
      <view bindtap="countLikes"></view>
    <view>
        <image src="{{countries.photo.path}}"></image>
    </view> 
      <view class="country-footer">
        <view>
          <image class="likebtn" src="/pages/show/img/heart.png"></image> 
        </view>
          <view class="likenum" id="likeId">
            {{countries.likednum}}
           </view>
      </view>
 
   </view>

  <view class="funfact-card">
  <view wx:for="{{funfacts}}">
  </view>
    <view class="card-title"> Funfact:</view>
  <view>{{item.description}}</view>
</view>




